import { FilterbygroupPipe } from './filterbygroup.pipe';

describe('FilterbygroupPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterbygroupPipe();
    expect(pipe).toBeTruthy();
  });
});
